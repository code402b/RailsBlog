<div id="feed">
	<h1>Post Feed:</h1>
	<% @posts.each do |post| %>
	  <div class="post"> 
	    <div class="post_title"> <%= post.title %></div>
	    <div class="post_body"> <%= post.body %> </div>

	    <%= button_to "Add Comment", new_post_comment_path(post) %>

	  </div>
	<% end %>
</div>